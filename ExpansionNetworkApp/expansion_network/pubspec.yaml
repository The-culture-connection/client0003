name: expansion_network
description: "MORTAR Alumni Network mobile app."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 5.0.0+16

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^14.6.2
  cached_network_image: ^3.4.1
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0
  firebase_messaging: ^16.0.4
  cloud_functions: ^6.0.0
  image_picker: ^1.1.2
  # Direct (normally transitive) so main.dart can flip `useAndroidPhotoPicker`, which
  # defaults to false. Without it the Android pick falls back to ACTION_GET_CONTENT and
  # the app needs READ_MEDIA_IMAGES — which Google Play's photo/video policy rejects.
  image_picker_android: ^0.8.13+16
  image_picker_platform_interface: ^2.11.1
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  video_player: ^2.9.5
  share_plus: ^13.0.0
  path_provider: ^2.1.5
  intl: ^0.20.2
  add_2_calendar: ^3.0.1
  uuid: ^4.5.1
  app_links: ^6.3.3
  tutorial_coach_mark: ^1.3.3
  shared_preferences: ^2.5.5
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.4.0
  vibration: ^3.2.0
  # Beta feedback: accelerometer for shake-to-report, package info to stamp
  # each report with the build it came from.
  sensors_plus: ^7.0.0
  # ^10 (not ^9): 9.x pins win32 ^5, which conflicts with share_plus ^13.
  package_info_plus: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  image_path: assets/MortarAppIcon.png
  android: true
  ios: true
  min_sdk_android: 23
  adaptive_icon_background: "#232323"
  adaptive_icon_foreground: assets/MortarAppIcon.png
  web:
    generate: true
    image_path: assets/MortarAppIcon.png
    background_color: "#232323"
    theme_color: "#232323"
  windows:
    generate: true
    image_path: assets/MortarAppIcon.png
  macos:
    generate: true
    image_path: assets/MortarAppIcon.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/welcome_mortarverse.gif
    - assets/conference/shop_storefront.png
    - assets/planets/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  fonts:
    - family: ArchivoBlack
      fonts:
        - asset: fonts/ArchivoBlack-Regular.ttf
    # The Mortarverse design language (Claude Design "Mortarverse Home.dc.html")
    # is set in Poppins. Weights limited to the five the designs actually use,
    # plus the three italics — the eyebrow, trailing hints and planet captions.
    - family: Poppins
      fonts:
        - asset: fonts/Poppins-Light.ttf
          weight: 300
        - asset: fonts/Poppins-LightItalic.ttf
          weight: 300
          style: italic
        - asset: fonts/Poppins-Regular.ttf
          weight: 400
        - asset: fonts/Poppins-Italic.ttf
          weight: 400
          style: italic
        - asset: fonts/Poppins-Medium.ttf
          weight: 500
        - asset: fonts/Poppins-MediumItalic.ttf
          weight: 500
          style: italic
        - asset: fonts/Poppins-SemiBold.ttf
          weight: 600
        - asset: fonts/Poppins-Bold.ttf
          weight: 700

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
