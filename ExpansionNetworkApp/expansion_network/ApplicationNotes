# Expansion Network — Firebase & data model

## Project

- **Firebase project:** `mortar-dev` (Mortar Dev), same as the digital curriculum web app.
- **Flutter config:** `lib/firebase_options.dart` (generated by FlutterFire CLI).
- **Onboarding data:** `lib/data/curriculum_onboarding_data.dart` — `kBusinessGoals`, `kCurriculumSkillCategories`, `kIndustries` match Digital Curriculum `src/app/lib/onboardingData.ts`.

## Authentication

- **Provider:** Firebase Auth — email / password. **First-time alumni access** uses a **one-time invite code** (Cloud Function `claimInviteAndCreateAccount`) then `signInWithCustomToken`; **returning users** sign in with email/password only.
- **Landing:** `/` — **Claim with invite** → `/auth/claim`, Sign in → `/auth/sign-in`.
- **Server gate:** After every sign-in, `AuthController` calls Cloud Function **`initializeUserSession`** (region `us-central1`). Access requires `eligibleUsers/{normalizedEmail}` with `networkAccess == true`. Auth alone or `users/{uid}` alone does **not** grant access.
- **Routing:** `AuthController` + `GoRouter` redirect:
  - Signed out users are limited to `/` and `/auth/*`. `/onboarding` requires sign-in.
  - After a successful claim or login, users who still need profile data go to `/onboarding` (not `/home`).
  - Signed-in users without completed onboarding are sent to `/onboarding` when opening shell routes.
  - **Profile gate:** `needsExpansionOnboarding` is `false` if `profileCreated == true` **and** `onboardingComplete == true`, or (legacy) `expansionOnboardingComplete == true`, or `onboarding_status == 'complete'` (curriculum web completion).

### How the feature works (invite + session)

- **Eligible roster:** Admins maintain `eligibleUsers/{normalizedEmail}` (and optional `cohortId`) via Mortar web **Admin → Expansion invites** (Next.js: `createOrUpdateEligibleUser`, `generateInviteCode`) and Digital Curriculum **Admin → App Access Hub** (same callables; per-student **Alumni app invite** uses role `Digital Curriculum Alumni` with a generated code). **Alumni Applications → Admit** also calls `createOrUpdateEligibleUser` with that role after curriculum admit. Server still exposes `bulkUploadEligibleUsers` and `promoteToDigitalCurriculumAlumni` for scripts or future tooling; the listed web UIs no longer surface bulk JSON or promote-only forms. Invite documents live in `inviteCodes/{inviteId}`; `eligibleUsers.latestInviteCodeId` points at the **current** code. Generating a new code **revokes** the previous invite doc (`revoked: true`).
- **Claim flow (`/auth/claim`):** Two steps: (1) **Email + invite code** → `validateInviteCode` (returns `authAccountExists`). (2a) **No Auth account yet:** password + confirm → `claimInviteAndCreateAccount` → `signInWithCustomToken`. (2b) **Auth account already exists:** **one** password field → `signInWithEmailAndPassword` → `finalizeInviteClaim` to consume the invite and link Firestore. **Edit email or code** returns to step 1. Race: if step 2a hits `ALREADY_EXISTS`, the UI switches to the single-password path.
- **Login flow:** Email/password sign-in → `initializeUserSession` syncs `role`, `networkAccess`, `normalizedEmail`, `lastLoginAt` into `users/{uid}` and returns `READY_FOR_HOME` vs `REQUIRES_ONBOARDING` vs `UNAUTHORIZED` (wrong or no eligible row, or student without network access).
- **Legacy cohort CSV:** `expansion_cohort_emails` import still sets cohort hints; it does **not** replace `eligibleUsers` for network authorization. See repo root `EXPANSION_INVITE_AUTH.md`.

### How onboarding works (7 steps, mirrors Digital Curriculum)

1. **Identity + location** — `first_name`/`last_name` (UI), city, state, cohort radio (in cohort + cohort ID, or not in cohort), bio, profession, email.
2. **Business goals** — same six strings as curriculum Step 2.
3. **Confident skills** — expandable categories, min 3 skills.
4. **Desired skills** — same UX, min 3 skills.
5. **Industry** — single choice from `kIndustries`.
6. **Work structure** — sliders: flexibility (1–10), weekly hours (20–80), ownership (1–10); stored as `work_structure`.
7. **Profile links** — LinkedIn, portfolio, Instagram, Facebook, TikTok (optional); **Complete profile** writes Firestore.

## Firestore — `users/{uid}` (curriculum-aligned)

On **first create** (`set`), the app also writes default nested structures so documents match the curriculum shape:

| Field | Type | Notes |
|-------|------|--------|
| `uid` | string | Auth uid |
| `email` | string | From Auth |
| `email_verified` | bool | From Auth |
| `first_name` | string | |
| `last_name` | string | |
| `city` | string | |
| `state` | string | |
| `bio` | string | |
| `profession` | string | |
| `not_in_cohort` | bool | |
| `cohort_id` | string | Set when in cohort; **deleted** on update if user selects “not in cohort” |
| `business_goals` | array of strings | Full labels |
| `confident_skills` | array of strings | Full skill labels |
| `desired_skills` | array of strings | Full skill labels |
| `industry` | string | One of `kIndustries` |
| `work_structure` | map | `flexibility`, `weekly_hours`, `ownership` (ints) |
| `profile_links` | map | `linkedin`, `portfolio`, `instagram`, `facebook`, `tiktok` |
| `onboarding_status` | string | `"complete"` |
| `profile_completed` | bool | `true` |
| `expansionOnboardingComplete` | bool | `true` (Expansion app gate) |
| `expansionOnboardingCompletedAt` | timestamp | |
| `profileCreated` | bool | `true` when onboarding finished (aligned with session callable) |
| `onboardingComplete` | bool | `true` when onboarding finished |
| `updated_at` | timestamp | Server |
| `display_name` | null | On create |
| `photo_url` | null | On create |
| `badges` | map | `{ earned: [], visible: [] }` on create |
| `membership` | map | `{ status: "active", paid_modules: [] }` on create |
| `points` | map | `{ balance: 0, history_summary: [] }` on create |
| `permissions` | map | `{ hidden_videos: [] }` on create |
| `roles` | array | `["Alumni"]` **only on create** |
| `created_at` | timestamp | **Only on create** |

**Updates** use `update` with `merge`-compatible fields; `roles` is not sent. Existing curriculum users keep their `roles` (e.g. `Digital Curriculum Students`).

## Feed tab — events + community posts

### How the feature works

- **Events** load from Firestore `events` (same schema as the digital curriculum admin events): `title`, `date`, `time`, `location`, `details`, `event_type`, `image_url`, `registered_users` (array of UIDs), optional `total_spots` / `available_spots`, `created_at`, `updated_at`. The list shows upcoming events (`date` on or after today). Tapping an event opens `/events/:eventId` (detail, register/unregister via `arrayUnion` / `arrayRemove` on `registered_users`).
- **Posts** use collection `feed_posts` with `post_category`, `post_title`, `post_details`, `author_id`, `author_name`, `created_at`, `updated_at`. Categories match the digital curriculum discussion list (`lib/data/feed_post_categories.dart`). **Likes** are stored under `feed_posts/{id}/likes/{userId}`; **replies** under `feed_posts/{id}/replies/{replyId}` with `body`, `author_id`, `author_name`, `created_at`. Reply likes use `replies/.../likes/{userId}`.
- **Routes:** `/feed/post/create` (new post), `/feed/post/:postId` (thread: full post, like, reply, like replies). FAB: **Posts** tab → new post; **Events** tab → `/events/create`; **All** tab → bottom sheet (new post or new event). **Opening the same post again** uses a unique query on the URL (`?_=<timestamp>`) and a `MaterialPage` keyed by the full URI so GoRouter does not reuse the same page key for identical paths (avoids the same post failing to open until much later).
- **Profile modal:** Tap a post author (avatar/name) on the feed or post detail to open a read-only profile sheet (same seven onboarding sections + achievements + certificates, no edit). **Message** is a full-width row under the title (not squeezed into the top bar) and opens `/messages/direct/{userId}` when signed in and the profile is not yours; auth is driven by `authStateChanges()` so the button appears once Firebase Auth has the user. Email is omitted when viewing someone else’s profile.
- **Events list:** Loads **every** document in `events` (no `orderBy` filter that drops docs missing `date`); sorted in the client by `date` when present.
- **Replies:** `author_name` is resolved from `users/{uid}` via `UserProfileRepository.getDisplayNameForUser`. Nested replies store optional `parent_reply_id` on the reply doc; UI shows threads with indent and **Reply** on each comment.

## Security rules note

See repo root `firestore.rules` — currently **development-wide** open rules on a date predicate for the shared project. **`eligibleUsers` and `inviteCodes` must not be writable from the client** in production; use Admin SDK in Cloud Functions only. Merging production rules must account for the digital curriculum web app in the same database. Details: `EXPANSION_INVITE_AUTH.md`.

Intended direction: `users/{userId}` **read** for any **authenticated** user (profile modal); owner write; feed/events rules as before.

## Development: Hot Restart vs Firebase (Android)

If you see:

`PlatformException(channel-error, Unable to establish connection on channel: ... FirebaseCoreHostApi.initializeCore ...)`

that usually happens after **Hot Restart**. Stop the app completely, then **Run** again (cold start). Prefer **Hot Reload** for UI work.

The same **Pigeon channel** break can appear when calling **Cloud Functions** (e.g. `validateInviteCode`, `initializeUserSession`):

`Unable to establish connection on channel: '...CloudFunctionsHostApi.call'`

That is **not** a bad invite or “stranger” flow logic — the Dart ↔ Android **MethodChannel** for `cloud_functions` is broken. Typical causes:

1. **Hot Restart (`R`)** on Android after Firebase/plugins loaded — use **Hot Reload (`r`)** only, or fully **stop** the app and **Run** again.
2. **APK never rebuilt** after adding `cloud_functions` — run **`flutter clean`**, **`flutter pub get`**, uninstall the app from the device/emulator, then **Run** so native `cloud_functions` code is packaged.
3. Rare emulator glitches — cold-boot the AVD or use a physical device.

Claim/sign-in screens map this to a short message via `userMessageForFirebaseCallableError` in `expansion_session_service.dart` (detection includes `cloud_functions_platform_interface` / `unknown` callable errors).

`lib/main.dart` skips `Firebase.initializeApp` when `Firebase.apps` is already populated, and logs a hint if init fails.
