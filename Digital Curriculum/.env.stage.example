# Staging: set these in Railway → Service → Variables (Vite bakes them in at build time).
# For local builds only, you can copy to .env.stage.
VITE_FIREBASE_ENV=stage

# Optional overrides (defaults come from firebase.ts `stage` block when env is stage)
# VITE_USE_EMULATOR=false
# VITE_FUNCTIONS_URL=
